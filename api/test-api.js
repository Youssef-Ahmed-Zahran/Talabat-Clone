import axios from 'axios';

async function main() {
  try {
    // We need a valid driver token. Since I can't get it easily, I'll bypass by querying the DB directly like the controller does.
  } catch (err) {}
}
